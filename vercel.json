{
    "functions": {
        "api/index.ts": {
            "includeFiles": "server/**"
        }
    },
    "rewrites": [
        {
            "source": "/api/(.*)",
            "destination": "/api/index.ts"
        }
    ],
    "buildCommand": "vite build",
    "outputDirectory": "dist/public",
    "framework": "vite"
}